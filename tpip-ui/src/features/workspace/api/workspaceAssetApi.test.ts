import{workspaceAssetApi}from'./workspaceAssetApi'
describe('workspace asset api',()=>{afterEach(()=>vi.unstubAllGlobals());it('sends stable filters and paging',async()=>{const fetchMock=vi.fn(async()=>new Response(JSON.stringify({items:[],page:0,size:20,totalElements:0,totalPages:0}),{status:200,headers:{'Content-Type':'application/json'}}));vi.stubGlobal('fetch',fetchMock);await workspaceAssetApi.workspaces({lifecycleStatus:'VERIFIED',riskLevel:'HIGH',environmentCode:' test ',keyword:' customer '});expect(fetchMock).toHaveBeenCalledWith(expect.stringMatching(/workspace-asset-views\?lifecycleStatus=VERIFIED&riskLevel=HIGH&environmentCode=test&keyword=customer&page=0&size=20$/),expect.any(Object))});it('loads detail evidence',async()=>{const f=vi.fn(async()=>new Response(JSON.stringify({workspace:{workspaceId:23},baselines:[],recentDriftReports:[],recentImpactEvidence:[]}),{status:200,headers:{'Content-Type':'application/json'}}));vi.stubGlobal('fetch',f);await workspaceAssetApi.workspace(23);expect(f).toHaveBeenCalledWith(expect.stringMatching(/workspace-asset-views\/23\?limit=20$/),expect.any(Object))})})
